#!/usr/bin/env perl
# $Id: pyx_xml_normalization,v 1.13 2005-11-14 17:04:32 skim Exp $

# Pragmas.
use strict;
use warnings;

# Modules.
use PYX::XMLNorm;

# Open file.
my $file_handler = *STDIN;
if ($ARGV[0]) {
	if (! open(INF, "<$ARGV[0]")) {
		die "Cannot open file '$ARGV[0]'.";
	}
	$file_handler = *INF;
}

# TODO
# Pridat flag pro flush stack :p

# PYX::XMLNorm object.
my $sorter = PYX::XMLNorm->new(
	'input_file_handler' => $file_handler,
	'rules' => {
		'tr' => ['th', 'td', 'tr'],
		'td' => ['td'],
		'th' => ['th'],
		'p' => ['p'],
		'div' => ['p'],
		'table' => ['td', 'tr'],
		'html' => ['body'],
		'*' => ['br', 'hr', 'link', 'meta', 'input', 'img']
	},
);
$sorter->parse_handler;

# Close file.
if ($ARGV[0]) {
	close(INF);
}

